
class ExecutionQueue {
public:
    struct Unit {
        time_t timeUntil;
        size_t id;
    };
    void add(time_t timeUntil, size_t id);
    bool empty();
    size_t wait();
    void getTimeToDelay();
    void getCurrId();
private:
    std::set<Unit> units_;
    time_t lastWakeUp = 0;
};
